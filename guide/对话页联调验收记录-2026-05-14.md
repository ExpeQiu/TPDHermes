# 对话页联调验收记录（2026-05-14）

## 验收目标

验证 `TPDHermes` 的 `/chat` 页面是否已完成以下联调闭环：

1. 前端可在发送前显式编排并展示项目、知识库、技能三类上下文摘要。
2. 前端默认通过本项目后端 `/api/v1/chat/completions` 发起聊天请求，而不是直接依赖外部地址。
3. 后端可代理到本机运行中的 `Hermes-agent`，并返回真实对话结果。
4. 页面能在同一次对话中同时展示“上下文注入摘要”和“Hermes-agent 回复”。

## 验收时间

- 日期：`2026-05-14`

## 验收范围

- 页面：`/chat`
- 前端入口：`src/app/chat/page.tsx`
- 上下文编排：`src/lib/chat-context.ts`
- 后端代理：`backend/routes/chat.py`
- Hermes-agent API：`http://localhost:8642/v1/chat/completions`

## 联调环境

- 操作系统：`macOS`
- 前端：`Next.js dev server`
- 后端：`FastAPI / Uvicorn`
- Agent：本机已运行 `Hermes-agent`
- Hermes-agent 鉴权：使用本机进程环境中的 `API_SERVER_KEY`

本次联调使用过两组临时服务地址，仅用于验证链路：

- 后端代理：`http://127.0.0.1:8011`、`http://127.0.0.1:8012`
- 前端页面：`http://127.0.0.1:3000/chat`、`http://127.0.0.1:3001/chat`

说明：

- 联调结束后，临时启动的前后端服务均已停止。
- 联调过程中创建的测试项目与本地 KB 缓存测试数据均已清理。
- 浏览器联调截图保留在 `tmp/chat-page-debug.png`，可作为本次验收证据。

## 验收前置确认

联调前已确认以下条件成立：

- 本机 `Hermes-agent` 正在监听 `127.0.0.1:8642`。
- 本项目后端健康接口可访问。
- 新增的 `/api/v1/chat/config` 与 `/api/v1/chat/completions` 路由可正常响应。
- `/chat` 页面默认链路已切换为本项目后端代理模式。

## 验收步骤

### 1. 后端代理验证

直接请求本项目后端聊天代理接口：

- 请求：`POST /api/v1/chat/completions`
- 消息内容：`请只回复：联调成功`

实际结果：

- 后端成功代理到本机 `Hermes-agent`。
- 返回内容为：`联调成功`

结论：

- 说明 `/api/v1/chat/completions` 已可用。
- 说明后端到 `Hermes-agent` 的代理链路成立。

### 2. 页面显式上下文编排验证

在真实浏览器中打开 `/chat` 页面后，执行以下操作：

1. 选择项目：`联调测试项目`
2. 选择知识库集合：`debug_docs`
3. 勾选技能快照注入
4. 输入消息：`请基于当前已注入的项目、知识库与技能快照，简要说明这个页面会在发送前准备哪些上下文。`
5. 点击发送

页面实际展示结果：

- 页面出现 `已注入 3 项显式上下文`
- 页面展示以下三个上下文块：
  - `mcp_tphermes_project_get`
  - `mcp_tphermes_kb_query`
  - `mcp_tphermes_workshop_list_skills`

### 3. 项目上下文验证

页面展示的项目上下文包含：

- 项目 ID
- 项目状态
- 项目描述
- 项目背景
- 受众
- 约束条件

结论：

- 项目上下文已在发送前显式获取并展示。

### 4. 知识库上下文验证

页面展示的知识库上下文包含：

- 集合名：`debug_docs`
- 来源：`cache`
- 命中数：`1`
- 命中文档摘要
- 相似度距离 `distance`

结论：

- 知识库检索结果已在发送前显式获取并展示。
- 当前环境下知识库通过本地缓存模式返回结果，链路可用。

### 5. 技能快照验证

页面展示的技能快照包含：

- 技能数：`4`
- 技能列表：
  - `hello_skill`
  - `speech_skill`
  - `video_skill`
  - `a4_skill`

结论：

- 技能快照已在发送前显式获取并展示。

### 6. Hermes-agent 回复验证

页面中 AI 最终返回的完整回复如下：

> 根据已注入的上下文，这个页面在发送前会准备三层上下文：
>
> 1. **项目详情（project_get）**
>    项目 ID、名称、描述、受众风格（简洁）、知识库需求标志（needKb: true）等元数据。
>
> 2. **知识库检索结果（kb_query）**
>    针对当前 query 从 debug_docs 集合检索命中的文档片段及相似度距离，这里命中 1 条。
>
> 3. **技能快照（workshop_list_skills）**
>    当前工坊中 4 个可用技能的名称列表：hello_skill、speech_skill、video_skill、a4_skill。
>
> 这三层上下文会一并注入给 Hermes-agent 作为回答时的参考依据。

结论：

- `Hermes-agent` 实际收到了前端显式注入的上下文。
- AI 回复内容与页面展示的三类上下文一致。
- 本次联调确认不仅“摘要展示成立”，而且“摘要已参与回答”。

## 验收结果

本次 `/chat` 页面联调结论为：**通过**。

通过口径如下：

- 前端已能显式展示发送前注入的项目、知识库、技能三类上下文。
- 前端默认已通过本项目后端统一承接聊天请求。
- 后端已可代理到本机 `Hermes-agent` 并返回真实回复。
- 页面已能在同一轮对话中同时展示“上下文注入摘要”和“AI 回答结果”。

## 当前已知限制

### 1. 知识库当前为缓存降级模式

页面中出现如下提示：

- `readonly cache mode - external KB unavailable`

这说明：

- 当前外部知识库服务未接通或不可用。
- 本次联调使用的是本地缓存结果，而非外部实时 KB。

影响判断：

- 不影响“前端显式上下文编排 + 后端代理 + Hermes-agent 回复”这条主链路的验收结论。
- 但“实时外部 KB 联调”仍需单独验收。

### 2. 本次为开发环境联调

本次验收基于本地开发服务完成，尚不代表生产部署环境已验收。

建议后续补充：

- 标准启动脚本下的回归记录
- Docker 环境联调记录
- 外部 KB 可用场景下的实时检索验收

## 验收结论

截至 `2026-05-14`，`TPDHermes` 的 `/chat` 页面已经完成“显式上下文注入 + 后端统一代理 + Hermes-agent 回复”的核心联调闭环，可作为当前阶段的已验收能力留档。
